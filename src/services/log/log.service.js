export const logError = (data, source) => {
  console.log(data);
  // console.log(`error-${source}`, data.message);
}

export const logInfo = (data) => {
  console.log(data);
  // console.log('info', data.message);
}